{
  "name": "dfx-raw",
  "license": "MIT",
  "version": "0.0.1",
  "scripts": {
    "prepare:arbitrum-one": "mustache ../../config/arbitrum-one.js template.yaml > subgraph.yaml",
    "codegen": "graph codegen",
    "build": "graph build",
    "create-local": "graph create --node http://localhost:8020/ dfx-raw",
    "remove-local": "graph remove --node http://localhost:8020/ dfx-raw",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 dfx-raw"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "^0.47.0",
    "@graphprotocol/graph-ts": "^0.30.0"
  },
  "devDependencies": { "matchstick-as": "0.5.0" }
}